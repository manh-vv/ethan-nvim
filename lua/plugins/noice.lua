return {
  "folke/noice.nvim",
  opts = {
    cmdline = {
      view = "cmdline", -- Changes the view to a classic cmdline at the bottom
      format = {
        cmdline = false,
        search_down = false,
        search_up = false,
        lua = false,
      },
    },
  },
}
